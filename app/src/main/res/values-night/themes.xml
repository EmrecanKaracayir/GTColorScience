<resources>

    <style name="Theme.GTColorScience" parent="Theme.Material3.Dark.NoActionBar">
        <!-- Transparent system bars for edge-to-edge. -->
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">?attr/isLightTheme</item>

        <item name="GTCS_backgroundColor">@color/dark_GTCS_backgroundColor</item>
        <item name="GTCS_foregroundColor">@color/dark_GTCS_foregroundColor</item>
    </style>
</resources>